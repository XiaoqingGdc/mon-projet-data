# Mon projet data
Ce projet est un script d'analyse de données avec un environnement virtuel structuré.

## Comment lancer le projet :
1. Installer les dépendances : `pip install -r requirements.txt`
2. Lancer le script : `python src/analyse.py`
